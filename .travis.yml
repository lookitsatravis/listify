language: php

sudo: false

php:
  - "7.4"
  - "7.3"
  - "7.2"

before_script:
  - composer self-update
  - composer install --dev --no-interaction

script:
  - mkdir -p build/logs
  - vendor/bin/phpunit

after_script:
  - php vendor/bin/coveralls
