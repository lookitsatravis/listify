language: php

sudo: false

php:
  - "7.3"
  - "7.2"

before_script:
  - composer self-update
  - composer install --dev --no-interaction

script:
  - mkdir -p build/logs
  - vendor/bin/phpunit

after_success:
  - travis_retry php vendor/bin/php-coveralls -v
